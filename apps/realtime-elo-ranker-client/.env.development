# --- CONFIGURATION API ---

# Option 1: Mock Server (Données fictives) -> pnpm apps:api-mock:start
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Option 2: Real Server NestJS (Vrai backend) -> pnpm apps:server:dev
# Decommentez la ligne ci-dessous et commentez celle du dessus pour utiliser le vrai serveur
NEXT_PUBLIC_API_BASE_URL=http://localhost:3002

